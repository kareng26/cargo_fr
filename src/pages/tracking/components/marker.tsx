import { styled } from "styled-components";
import { Marker as MC } from "react-map-gl";

const Marker = styled(MC)`
    display: flex;
    flex-direction: column;
    align-items: center;
`;

export { Marker };
