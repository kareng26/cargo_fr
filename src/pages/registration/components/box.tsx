import styled from "styled-components";
import { Box as BC } from "@mui/material";

const Box = styled(BC)`
    display: flex;
    padding: 10px;
    width: 100%;
    max-width: 360px;
    flex-direction: column;
    align-items: center;
`;

export { Box };
